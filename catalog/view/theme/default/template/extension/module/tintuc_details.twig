<hr/>
<h3 class="category-item-name"><strong>{{ moduleName }}</strong></h3>
<div class="swiper-viewport no-border">
  <div id="carouselkhacdau{{ module }}" class="swiper-container">
      {% for product in products %}
       <h4><a href="{{ product.href }}">{{ product.name }} ({{ product.date_added |date("d/m/Y") }}) </a></h4>
      {% endfor %}
      
  </div>
</div>
